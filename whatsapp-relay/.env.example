# WhatsApp Relay Configuration

# WebSocket server port for iOS app connection
WS_PORT=8080

# Health check HTTP port
HTTP_PORT=8081

# Allowed iOS app IP (local network only). Use * for any.
ALLOWED_IP=*

# WhatsApp target JID (the phone number that sends commands via Ray-Ban glasses)
# Format: <country_code><number>@s.whatsapp.net
# Example: 447700900000@s.whatsapp.net
TARGET_JID=

# Log level: fatal, error, warn, info, debug, trace
LOG_LEVEL=info
